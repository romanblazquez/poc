import { InjectionToken } from '@angular/core';

/** Default category tag for log entries emitted by this app. */
export const INTEROP_LOG_CATEGORY = new InjectionToken<string>('INTEROP_LOG_CATEGORY');
