{
  "name": "payment-action",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/payment-action/src",
  "projectType": "application",
  "tags": ["type:app", "scope:renderer"],
  "targets": {
    "serve": { "executor": "nx:run-commands", "options": { "command": "vite --config apps/payment-action/vite.config.ts", "cwd": "{workspaceRoot}" } },
    "build": { "executor": "nx:run-commands", "options": { "command": "vite build --config apps/payment-action/vite.config.ts", "cwd": "{workspaceRoot}" } }
  }
}
