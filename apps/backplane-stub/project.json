{
  "name": "backplane-stub",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backplane-stub/src",
  "projectType": "application",
  "tags": ["type:app", "scope:node", "scope:stub"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx apps/backplane-stub/src/main.ts {args}",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/backplane-stub/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
