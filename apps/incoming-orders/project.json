{
  "name": "incoming-orders",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/incoming-orders/src",
  "projectType": "application",
  "tags": ["type:app", "scope:renderer"],
  "targets": {
    "serve": { "executor": "nx:run-commands", "options": { "command": "vite --config apps/incoming-orders/vite.config.ts", "cwd": "{workspaceRoot}" } },
    "build": { "executor": "nx:run-commands", "options": { "command": "vite build --config apps/incoming-orders/vite.config.ts", "cwd": "{workspaceRoot}" } }
  }
}

